# CLAUDE.md

這個 repo 是**發票 MCP 的公開安裝說明文件**。不含服務端原始碼，讀者是**一般台灣消費者**（發票存摺 App 使用者），不是工程師。

## 動筆前先讀這個

**[`docs/maintenance.md`](docs/maintenance.md) 是本 repo 所有寫作規則的 SoT。改任何文件前先讀它。**

那份文件裡的每一條規則都來自一次實際犯過的錯，包含錯誤本身與為什麼。**不要憑常識覆蓋它** —— 看起來「順手補齊比較完整」的動作，多半正是它明文要求不要做的。

## 最常被違反的四條

以下只是索引，完整理由在 `docs/maintenance.md` 對應章節：

1. **不要列「支援／不支援哪些 AI 工具」的清單。** 各家 MCP 支援每個月都在變，寫死的清單一定過期，而過期的清單就是誤導 —— 尤其「不支援」這種負面斷言會直接叫使用者放棄。改為給判斷條件。→ 〈不要寫「不支援清單」〉
2. **非安裝段落不要列舉工具名。** 判斷標準：拿掉工具名後讀者還知道這句適用於他嗎？還知道就是多餘。→ 〈非必要不列舉 AI 工具名〉
3. **`.github/` 底下的連結一律用絕對 URL。** issue／PR 模板會被渲染在 repo 之外，相對路徑在那裡會解析到錯的地方。→ 〈連結規範〉
4. **有一節叫〈刻意不做的事〉。** `CHANGELOG.md`、`CODE_OF_CONDUCT.md`、`examples/`、demo GIF、`CONTRIBUTING.md` 都是**刻意不做**的，附了對標 34 個專案的普及率與理由。**看到缺就補會直接違反它。**

## 硬規則

- **金鑰（`inv_` 開頭）絕對不進 repo**，任何檔案、任何範例都不行。範例一律寫 `inv_你的金鑰`。
- **本 repo 不收外部 PR。** 不要新增任何暗示歡迎外部貢獻的內容。
- **`README.md` 是安裝說明的唯一 SoT。** 不要在其他地方複製安裝指令原文 —— 複製過去就一定會漂移。
- 對使用者說話用「**發票存摺**」或「我們」／「本服務」，**不要用法人名**。例外只有 `LICENSE` 的 Copyright 行。
- 截圖有規格（來源 600px、顯示鎖 300px、用 `<img>` 不用 `![]()`）。→ 〈截圖規格〉

## 語言

繁體中文（台灣用語）。程式碼識別字、指令、專有名詞維持原文。

## 改文件之後

「支援哪些 client」這件事同時存在於本 repo 的 `README.md` 和 App 內的設定教學頁 —— **兩者必須同一個 sprint 內一起改**，這是唯一會漂移的重疊項。詳見〈支援清單同步規則〉。
